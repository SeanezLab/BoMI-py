import sys

# Add the 'yost' directory to path. This is because the yost API is designed
# to be placed in the root directory, which is bad practice and increases clutter
# sys.path.insert(0, "yost")
